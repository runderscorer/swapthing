<div id='wishlist'>
  <h2>
    <%= "#{@wishlist.user.full_name}'s Wishlist" %>
  </h2>

  <%= @wishlist.show_instructions_or_reminder %>

  <%= react_component('ShowWishlistItems', 
    props: {
      items: @wishlist_items,
      isOwner: false,
    }) 
  %>
</div>