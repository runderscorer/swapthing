<%= simple_form_for [@wishlist, @item], url: {action: 'create'} do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :price %>
  <%= f.input :notes %>
  <%= f.input :url %>
  <%= f.submit 'Submit' %>
<% end %>